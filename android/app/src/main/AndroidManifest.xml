<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Android TV: touchscreen is not available on TV, mark as not required -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <!-- Android TV: declare Leanback support (required=false = runs on both TV and phone) -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <!-- Android TV: TVs don't support portrait. Without this, aapt implicitly marks portrait
         as required (because MainActivity has no fixed screenOrientation), causing install failure. -->
    <uses-feature android:name="android.hardware.screen.portrait" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Cineflix"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/tv_banner"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cineflix"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Google Cast Options Provider (Chromecast) -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.cineflix.android.CastOptionsProvider" />

        <!-- Main Compose Activity -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|uiMode"
            android:launchMode="singleTask"
            android:exported="true"
            android:alwaysRetainTaskState="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Standard phone/tablet launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Android TV launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fullscreen video player (TVGram approach - file:// direct) -->
        <activity
            android:name=".ui.player.PlayerActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:exported="false" />

    </application>

</manifest>
